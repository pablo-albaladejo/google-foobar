package level5.disorderlyescape;

import java.math.BigInteger;

public class Solution {
    public static String solution(int w, int h, int s){
        BigInteger numConfigs = BigInteger.valueOf(430);
        // TODO: implement logic to calculate the number of unique, non-equivalent configurations
        return numConfigs.toString();
    }
}
